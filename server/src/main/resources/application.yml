spring:
  application:
    name: server
server:
  port: 18080
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    serviceUrl:
      defaultZone: http://localhost:18080/eureka
